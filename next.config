// next.config.js
const path = require("path");

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // IMPORTANT: never overwrite config.resolve/alias — merge instead.
  webpack: (config) => {
    config.resolve = config.resolve || {};
    config.resolve.alias = {
      ...(config.resolve.alias || {}),
      // Keep Next's internal aliases, just add yours:
      "@": path.resolve(__dirname), // matches your jsconfig "@/*": ["*"]
    };
    return config;
  },
};

module.exports = nextConfig;
